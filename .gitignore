# This .gitignore file is used to specify files and directories that should be ignored by Git when committing changes to the repository. It helps to keep the repository clean by excluding files that are not necessary to track, such as build artifacts, temporary files, and other generated content.

# Ignore the "bin" directory, which is commonly used to store compiled binaries.
learning_GO 

# Ignoring the secrets
.env

# tmp created by air.
tmp
tmp/*